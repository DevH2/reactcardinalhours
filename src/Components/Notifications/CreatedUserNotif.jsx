import { Snackbar, SnackbarContent } from "@material-ui/core"

export default const CreatedUserNotif = (props) => {
    <Snackbar>
        <SnackbarContent>
            
        </SnackbarContent>
    </Snackbar>
}